file(GLOB extension_dirs CONFIGURE_DEPENDS "*")
foreach(extension_dir ${extension_dirs})
  if(EXISTS "${extension_dir}/CMakeLists.txt")
    addextension(${extension_dir})
  endif()
  if(extension_dir MATCHES ".cmake$")
    include(${extension_dir})
  endif()
endforeach()
